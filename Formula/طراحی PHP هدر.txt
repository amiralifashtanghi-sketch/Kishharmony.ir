<?php
// 1. استایل‌ها (تزریق به هد سایت)
add_action('wp_head', 'kishharmony_custom_header_css');
function kishharmony_custom_header_css() {
    ?>
    <link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazir-font@v30.1.0/dist/font-face.css" rel="stylesheet" type="text/css" />
    <style>
        /* متغیرهای رنگی سازمانی کیش هارمونی */
        :root {
            --kh-primary: #1e3a8a;   /* 60%: آبی پررنگ (سرمه‌ای) */
            --kh-secondary: #00b4d8; /* 30%: فیروزه‌ای */
            --kh-accent: #f97316;    /* 10%: نارنجی */
            --kh-text: #1e293b;
            --kh-bg: #f8fafc;
        }

        /* استایل‌های پایه */
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Vazir', system-ui, -apple-system, sans-serif; background-color: var(--kh-bg); color: var(--kh-text); line-height: 1.5; padding-top: 70px; transition: padding-top 0.8s cubic-bezier(0.45, 0, 0.55, 1.0); }
        body.header-scrolled { padding-top: 72px; }

        /* هدر اصلی */
        .header { position: fixed; top: 0; left: 0; width: 100%; z-index: 1000; display: flex; align-items: center; justify-content: space-between; padding: 0 1.8rem; height: 70px; background: transparent; border: 1px solid transparent; border-radius: 0; box-shadow: none; backdrop-filter: none; -webkit-backdrop-filter: none; transition: all 0.8s cubic-bezier(0.45, 0, 0.55, 1.0); }
        .header.scrolled { background: rgba(255, 255, 255, 0.25); backdrop-filter: blur(28px) saturate(200%); -webkit-backdrop-filter: blur(28px) saturate(200%); border-radius: 50px; top: 14px; left: 3%; width: 94%; height: 58px; padding: 0 2.2rem; border: 1px solid rgba(255, 255, 255, 0.4); box-shadow: 0 20px 40px -12px rgba(30, 58, 138, 0.1), 0 4px 12px rgba(30, 58, 138, 0.05), inset 0 1px 0 rgba(255,255,255,0.6); }
        .header__nav-area { display: flex; align-items: center; gap: 1rem; flex: 1; justify-content: flex-start; }
        .header__center { display: flex; justify-content: center; align-items: center; margin: 0 auto; }
        .header__right { display: flex; align-items: center; gap: 1rem; }

        @media screen and (min-width: 769px) {
            .header { justify-content: space-between; }
            .header__nav-area { justify-content: center; flex: 2; }
            .header__center { order: -1; margin: 0; margin-left: auto; justify-content: flex-end; flex: 0 0 auto; }
            .header__right { order: 1; flex: 0 0 auto; }
        }

        /* لوگو */
        .header-logo { display: flex; align-items: center; text-decoration: none; color: var(--kh-primary); white-space: nowrap; font-weight: 700; font-size: 1.4rem; letter-spacing: -0.5px; text-shadow: 0 2px 8px rgba(0,0,0,0.05); transition: color 0.3s; }
        .header-logo:hover { color: var(--kh-secondary); }

        /* ======================= منوی دسکتاپ و زیرمنوها ======================= */
        ul.desktop-nav { display: flex; gap: 1.8rem; list-style: none; margin: 0; padding: 0; align-items: center; }
        ul.desktop-nav li { position: relative; list-style: none; }
        ul.desktop-nav a { text-decoration: none; color: var(--kh-primary); font-weight: 600; font-size: 0.95rem; transition: color 0.2s; display: flex; align-items: center; gap: 4px; padding: 0.5rem 0; }
        ul.desktop-nav a:hover, ul.desktop-nav li.current-menu-item > a { color: var(--kh-secondary); }

        /* فلش کوچک برای موارد دارای زیرمنو در دسکتاپ */
        ul.desktop-nav .menu-item-has-children > a::after { content: '▾'; font-size: 0.8rem; transition: transform 0.3s ease; }
        ul.desktop-nav .menu-item-has-children:hover > a::after { transform: rotate(180deg); }

        /* استایل گلاس‌مورفیسم زیرمنوی دسکتاپ */
        ul.desktop-nav .sub-menu { position: absolute; top: 100%; right: 0; min-width: 210px; background: rgba(255, 255, 255, 0.9); backdrop-filter: blur(20px) saturate(180%); -webkit-backdrop-filter: blur(20px) saturate(180%); border-radius: 16px; box-shadow: 0 15px 35px rgba(30, 58, 138, 0.1), 0 4px 10px rgba(30, 58, 138, 0.05); padding: 0.6rem; list-style: none; opacity: 0; visibility: hidden; transform: translateY(12px); transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1); border: 1px solid rgba(255, 255, 255, 0.8); z-index: 1100; display: flex; flex-direction: column; gap: 2px; }
        ul.desktop-nav li:hover > .sub-menu { opacity: 1; visibility: visible; transform: translateY(0); }
        ul.desktop-nav .sub-menu a { padding: 0.6rem 1rem; color: var(--kh-primary); font-size: 0.88rem; font-weight: 500; border-radius: 10px; transition: all 0.2s ease; white-space: nowrap; }
        ul.desktop-nav .sub-menu a:hover { background: rgba(0, 180, 216, 0.1); color: var(--kh-secondary); padding-right: 1.3rem; }

        /* همبرگر */
        .hamburger { display: none; flex-direction: column; justify-content: center; width: 36px; height: 36px; padding: 6px; background: none; border: none; cursor: pointer; position: relative; gap: 6px; }
        .hamburger span { display: block; width: 100%; height: 2.5px; background-color: var(--kh-primary); border-radius: 3px; transition: all 0.3s ease; transform-origin: center; }
        .hamburger.active span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
        .hamburger.active span:nth-child(2) { opacity: 0; transform: scaleX(0); }
        .hamburger.active span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

        /* آیکون‌ها */
        .header__icon { display: flex; align-items: center; justify-content: center; width: 38px; height: 38px; border-radius: 12px; background: transparent; color: var(--kh-primary); transition: background 0.3s ease, color 0.3s ease; text-decoration: none; position: relative; }
        .header__icon svg { width: 22px; height: 22px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
        .header__icon:hover { background: rgba(0, 180, 216, 0.1); color: var(--kh-secondary); }
        .cart-count { position: absolute; top: -4px; right: -4px; background: var(--kh-accent); color: white; font-size: 0.65rem; width: 18px; height: 18px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; box-shadow: 0 2px 5px rgba(249, 115, 22, 0.4); }

        /* ======================= منوی موبایل و آکاردئون ======================= */
        .mobile-menu-overlay { position: fixed; inset: 0; background: rgba(30, 58, 138, 0.6); backdrop-filter: blur(4px); z-index: 2000; opacity: 0; visibility: hidden; transition: opacity 0.3s, visibility 0.3s; }
        .mobile-menu-overlay.active { opacity: 1; visibility: visible; }
        .mobile-menu { position: fixed; top: 0; right: -320px; width: 300px; height: 100vh; background: #ffffff; z-index: 2100; box-shadow: -10px 0 40px rgba(0,0,0,0.15); transition: right 0.4s cubic-bezier(0.25, 0.8, 0.25, 1); overflow-y: auto; display: flex; flex-direction: column; border-left: 4px solid var(--kh-secondary); }
        .mobile-menu.active { right: 0; }
        .mobile-menu__header { padding: 1.5rem; background: #ffffff; border-bottom: 1px solid #f1f5f9; display: flex; align-items: center; justify-content: space-between; }
        .mobile-menu__logo { font-weight: 700; color: var(--kh-primary); font-size: 1.3rem; }
        .mobile-menu__close, .cart-panel__close { background: none; border: none; font-size: 1.8rem; cursor: pointer; color: var(--kh-primary); width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; border-radius: 12px; transition: background 0.2s, color 0.2s; }
        .mobile-menu__close:hover, .cart-panel__close:hover { background: rgba(0, 180, 216, 0.1); color: var(--kh-secondary); }
        
        ul.mobile-nav { list-style: none; padding: 1rem 1.2rem; flex: 1; }
        ul.mobile-nav li { border-bottom: 1px solid #f1f5f9; list-style: none; position: relative; }
        ul.mobile-nav li:last-child { border-bottom: none; }
        ul.mobile-nav .menu-item-has-children { display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; }

        ul.mobile-nav a { text-decoration: none; color: var(--kh-primary); font-size: 1.05rem; font-weight: 600; display: block; padding: 0.9rem 0.8rem; border-radius: 10px; transition: all 0.25s ease; flex: 1; position: relative; }
        
        /* نوار راهنمای کناری فیروزه‌ای در موبایل */
        ul.mobile-nav > li > a::before { content: ''; position: absolute; right: 0; top: 50%; transform: translateY(-50%); width: 3px; height: 0; background-color: var(--kh-secondary); border-radius: 3px; transition: height 0.3s ease; }
        ul.mobile-nav a:hover { color: var(--kh-secondary); background: rgba(0, 180, 216, 0.05); padding-right: 1.5rem; }
        ul.mobile-nav a:hover::before { height: 60%; }

        /* دکمه فلش بازکننده زیرمنوی موبایل */
        .submenu-toggle { background: none; border: none; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; cursor: pointer; color: var(--kh-primary); font-size: 0.85rem; transition: transform 0.3s ease, color 0.3s ease; border-radius: 8px; }
        .submenu-toggle.active { transform: rotate(180deg); color: var(--kh-secondary); }

        /* زیرمنو موبایل */
        ul.mobile-nav .sub-menu { display: none; width: 100%; list-style: none; padding: 0.2rem 1rem 0.6rem 0; margin-top: -0.3rem; background: rgba(0, 180, 216, 0.03); border-radius: 10px; }
        ul.mobile-nav .sub-menu.open { display: block; }
        ul.mobile-nav .sub-menu a { font-size: 0.95rem; padding: 0.6rem 0.8rem; color: var(--kh-primary); font-weight: 500; }
        ul.mobile-nav .sub-menu a::before { display: none; }

        /* پنل سبد خرید */
        .cart-panel-overlay { position: fixed; inset: 0; background: rgba(30, 58, 138, 0.6); backdrop-filter: blur(4px); z-index: 2000; opacity: 0; visibility: hidden; transition: opacity 0.3s, visibility 0.3s; }
        .cart-panel-overlay.active { opacity: 1; visibility: visible; }
        .cart-panel { position: fixed; top: 0; left: -380px; width: 360px; height: 100vh; background: #ffffff; z-index: 2100; box-shadow: 10px 0 40px rgba(0,0,0,0.15); transition: left 0.4s cubic-bezier(0.25, 0.8, 0.25, 1); overflow-y: auto; padding: 2rem 1.5rem; }
        .cart-panel.active { left: 0; }
        .cart-panel__header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1.5rem; border-bottom: 1px solid #f1f5f9; padding-bottom: 1rem; }
        .cart-panel h3 { font-weight: 700; margin: 0; color: var(--kh-primary); }
        
        .cart-panel .widget_shopping_cart_content { margin-top: 1rem; }
        .cart-panel ul.cart_list li { display: flex; gap: 1rem; align-items: center; border-bottom: 1px solid #f1f5f9; padding: 1rem 0; margin:0; }
        .cart-panel ul.cart_list li img { width: 60px; height: 60px; object-fit: cover; border-radius: 8px; box-shadow: none; margin:0; }
        .cart-panel ul.cart_list li a { color: var(--kh-primary); font-weight: 600; text-decoration: none; font-size: 0.95rem; transition: color 0.2s; }
        .cart-panel ul.cart_list li a:not(.remove):hover { color: var(--kh-secondary); }
        .cart-panel ul.cart_list li .quantity { color: var(--kh-secondary); font-weight: bold; margin-top: 0.3rem; display:block; }
        .cart-panel ul.cart_list li a.remove { color: #ef4444; font-weight: bold; border-radius: 50%; display: flex; justify-content: center; align-items: center; }
        .cart-panel .woocommerce-mini-cart__total { border-top: none; padding-top: 1.5rem; text-align: center; font-weight: bold; font-size: 1.1rem; color: var(--kh-primary); }
        .cart-panel .woocommerce-mini-cart__buttons a { display: block; width: 100%; text-align: center; background: rgba(30, 58, 138, 0.05); color: var(--kh-primary); padding: 0.8rem; border-radius: 8px; margin-bottom: 0.5rem; text-decoration: none; font-weight: bold; transition: 0.3s; }
        
        /* دکمه پرداخت - ۱۰٪ نارنجی برای Call to Action */
        .cart-panel .woocommerce-mini-cart__buttons a.checkout { background: var(--kh-accent); color: white; box-shadow: 0 4px 15px rgba(249, 115, 22, 0.3); }
        .cart-panel .woocommerce-mini-cart__buttons a:hover { opacity: 0.85; transform: translateY(-1px); }

        /* واکنش‌گرایی */
        @media screen and (max-width: 768px) {
            body { padding-top: 62px; }
            body.header-scrolled { padding-top: 60px; }
            .header { height: 62px; padding: 0 1rem; }
            .header.scrolled { border-radius: 35px; top: 8px; left: 2%; width: 96%; height: 52px; padding: 0 1.5rem; }
            ul.desktop-nav { display: none !important; }
            .hamburger { display: flex; }
            .header__center { margin: 0 auto; order: 0; }
            .header__nav-area { flex: 0; justify-content: flex-start; }
            .header-logo { font-size: 1.2rem; }
        }
        @media screen and (max-width: 480px) {
            .header.scrolled { border-radius: 28px; top: 6px; }
            .header-logo { font-size: 1.1rem; }
        }
    </style>
    <?php
}

// 2. ساختار HTML
add_action('wp_body_open', 'kishharmony_custom_header_html');
function kishharmony_custom_header_html() {
    $cart_count = 0;
    if ( class_exists( 'WooCommerce' ) && ! is_null( WC()->cart ) ) {
        $cart_count = WC()->cart->get_cart_contents_count();
    }
    
    $my_account_link = class_exists('WooCommerce') ? get_permalink( get_option('woocommerce_myaccount_page_id') ) : '#';
    ?>
    <header class="header" id="header">
        <div class="header__nav-area">
            <button class="hamburger" id="hamburgerBtn" aria-label="منو">
                <span></span><span></span><span></span>
            </button>
            <?php
            // فراخوانی فهرست ۱
            wp_nav_menu( array(
                'menu'           => 'فهرست ۱',
                'container'      => false,
                'menu_class'     => 'desktop-nav',
                'menu_id'        => 'desktopNav',
                'fallback_cb'    => false,
            ) );
            ?>
        </div>

        <div class="header__center">
            <a href="<?php echo esc_url( home_url('/') ); ?>" class="header-logo">کیش هارمونی</a>
        </div>

        <div class="header__right">
            <a href="#" class="header__icon" id="cartIcon" aria-label="سبد خرید">
                <svg viewBox="0 0 24 24"><path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4zM3 6h18" /><circle cx="8" cy="10" r="2" /><circle cx="16" cy="10" r="2" /></svg>
                <span class="cart-count custom-cart-count"><?php echo esc_html($cart_count); ?></span>
            </a>
            <a href="<?php echo esc_url($my_account_link); ?>" class="header__icon" aria-label="حساب کاربری">
                <svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="4" /><path d="M20 21a8 8 0 10-16 0" /></svg>
            </a>
        </div>
    </header>

    <!-- منوی موبایل -->
    <div class="mobile-menu-overlay" id="mobileMenuOverlay"></div>
    <div class="mobile-menu" id="mobileMenu">
        <div class="mobile-menu__header">
            <span class="mobile-menu__logo">کیش هارمونی</span>
            <button class="mobile-menu__close" id="closeMobileMenu">✕</button>
        </div>
        <?php
        wp_nav_menu( array(
            'menu'           => 'فهرست ۱',
            'container'      => false,
            'menu_class'     => 'mobile-nav',
            'menu_id'        => 'mobileNav',
            'fallback_cb'    => false,
        ) );
        ?>
    </div>

    <!-- سبد خرید -->
    <div class="cart-panel-overlay" id="cartPanelOverlay"></div>
    <div class="cart-panel" id="cartPanel">
        <div class="cart-panel__header">
            <h3>سبد خرید</h3>
            <button class="cart-panel__close" id="closeCartPanel">✕</button>
        </div>
        <div class="custom-cart-widget-area">
            <?php 
            if ( class_exists('WooCommerce') ) {
                the_widget( 'WC_Widget_Cart', 'title=' ); 
            } else {
                echo '<p>افزونه ووکامرس فعال نیست.</p>';
            }
            ?>
        </div>
    </div>
    <?php
}

// 3. جاوا اسکریپت (تزریق به فوتر)
add_action('wp_footer', 'kishharmony_custom_header_js');
function kishharmony_custom_header_js() {
    ?>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const header = document.getElementById('header');
            const hamburgerBtn = document.getElementById('hamburgerBtn');
            const mobileMenu = document.getElementById('mobileMenu');
            const mobileMenuOverlay = document.getElementById('mobileMenuOverlay');
            const closeMobileMenuBtn = document.getElementById('closeMobileMenu');
            const cartIcon = document.getElementById('cartIcon');
            const cartPanel = document.getElementById('cartPanel');
            const cartPanelOverlay = document.getElementById('cartPanelOverlay');
            const closeCartPanelBtn = document.getElementById('closeCartPanel');

            // ۱. تغییر وضعیت اسکرول
            function updateHeaderState() {
                if (window.scrollY > 10) {
                    header.classList.add('scrolled');
                    document.body.classList.add('header-scrolled');
                } else {
                    header.classList.remove('scrolled');
                    document.body.classList.remove('header-scrolled');
                }
            }
            window.addEventListener('scroll', updateHeaderState, { passive: true });
            updateHeaderState();

            // ۲. اضافه کردن دکمه فلش و اکاردئون برای زیرمنوهای موبایل
            const mobileMenuItems = document.querySelectorAll('.mobile-nav .menu-item-has-children');
            mobileMenuItems.forEach(item => {
                const toggleBtn = document.createElement('button');
                toggleBtn.className = 'submenu-toggle';
                toggleBtn.innerHTML = '▼';
                toggleBtn.setAttribute('type', 'button');
                toggleBtn.setAttribute('aria-label', 'باز کردن زیرمنو');
                
                const mainLink = item.querySelector(':scope > a');
                if (mainLink) {
                    mainLink.after(toggleBtn);
                }

                const subMenu = item.querySelector(':scope > .sub-menu');

                toggleBtn.addEventListener('click', (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    toggleBtn.classList.toggle('active');
                    if (subMenu) {
                        subMenu.classList.toggle('open');
                    }
                });
            });

            // ۳. منوی همبرگری
            if(hamburgerBtn) {
                hamburgerBtn.addEventListener('click', () => {
                    mobileMenu.classList.toggle('active');
                    mobileMenuOverlay.classList.toggle('active');
                    hamburgerBtn.classList.toggle('active');
                });
            }

            function closeMobileMenu() {
                if(mobileMenu) mobileMenu.classList.remove('active');
                if(mobileMenuOverlay) mobileMenuOverlay.classList.remove('active');
                if(hamburgerBtn) hamburgerBtn.classList.remove('active');
            }

            if(closeMobileMenuBtn) closeMobileMenuBtn.addEventListener('click', closeMobileMenu);
            if(mobileMenuOverlay) mobileMenuOverlay.addEventListener('click', closeMobileMenu);

            // ۴. پنل سبد خرید
            function openCartPanel() {
                cartPanel.classList.add('active');
                cartPanelOverlay.classList.add('active');
            }
            function closeCartPanel() {
                cartPanel.classList.remove('active');
                cartPanelOverlay.classList.remove('active');
            }

            if(cartIcon) {
                cartIcon.addEventListener('click', (e) => {
                    e.preventDefault();
                    openCartPanel();
                });
            }
            if(closeCartPanelBtn) closeCartPanelBtn.addEventListener('click', closeCartPanel);
            if(cartPanelOverlay) cartPanelOverlay.addEventListener('click', closeCartPanel);

            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape') {
                    closeMobileMenu();
                    closeCartPanel();
                }
            });
        });
    </script>
    <?php
}

// 4. به روزرسانی زنده شمارشگر سبد خرید
add_filter( 'woocommerce_add_to_cart_fragments', 'kishharmony_cart_count_fragments', 10, 1 );
function kishharmony_cart_count_fragments( $fragments ) {
    if ( class_exists( 'WooCommerce' ) && ! is_null( WC()->cart ) ) {
        $count = WC()->cart->get_cart_contents_count();
        $fragments['span.custom-cart-count'] = '<span class="cart-count custom-cart-count">' . esc_html( $count ) . '</span>';
    }
    return $fragments;
}